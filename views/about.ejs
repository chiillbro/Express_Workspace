<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About</title>
  </head>
  <body>
    <h1>My Name : <%= name %></h1>
    <% if (id === 30) { %>
    <h2>My ID : <%= id %></h2>
    <% } else if (id === 40) { %>
    <h5>My ID : <%= id %></h5>
    <% } else { %>
    <h1>No ID</h1>
    <% } %>
  </body>
</html>
